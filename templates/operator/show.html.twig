{% extends 'base.html.twig' %}

{% block title %}Operator{% endblock %}

{% block body %}
    <h1>Operator</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ operator.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ operator.name }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ operator.lastName }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ operator.gender }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_operator_index') }}">back to list</a>

    <a href="{{ path('app_operator_edit', {'id': operator.id}) }}">edit</a>

    {{ include('operator/_delete_form.html.twig') }}
{% endblock %}
