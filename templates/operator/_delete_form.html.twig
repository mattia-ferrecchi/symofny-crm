<form method="post" action="{{ path('app_operator_delete', {'id': operator.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ operator.id) }}">
    <button class="btn">Delete</button>
</form>
